- name: Sync static site to S3
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    site_dir: "../../site"

  tasks:
    - name: Upload static website files to S3
      command: aws s3 sync {{ site_dir }} s3://{{ bucket_name }} --delete

